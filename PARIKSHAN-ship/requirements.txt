# PARIKSHAN — pinned dependencies, all OSI-approved licences (see LICENSES.md)
# Python 3.12.10
#
# CORE deps for Phases 1-6 (data, features, models, dashboard). Install now.
# Phase 7 (LLM assistant) deps live in requirements-assistant.txt and are
# installed only when Phase 7 begins — see HANDOFF.md Phase 1 report for why
# (chromadb/sentence-transformers pull in a native-build dependency and a
# multi-GB torch install neither of which Phases 1-6 need).

# Core dataframes / IO
pandas==2.2.3
numpy==1.26.4
pyarrow==17.0.0

# Classical statistics
statsmodels==0.14.4
scipy==1.14.1
lifelines==0.29.0

# Machine learning
scikit-learn==1.5.2
xgboost==2.1.2
lightgbm==4.5.0
shap==0.46.0
joblib==1.6.0

# Dashboard
streamlit==1.39.0
plotly==5.24.1

# Testing & quality
pytest==8.3.3
pytest-cov==5.0.0
ruff==0.7.4

# Notebook execution (for DoD nbconvert check)
jupyter==1.1.1
nbconvert==7.16.4
ipykernel==6.29.5
# Pinned below <3.1: nbconvert 7.16.4's HTML exporter subclasses mistune's
# BlockParser with a SPECIFICATION entry ("axt_heading") that newer mistune
# (3.1+, e.g. the 3.3.4 pulled in transitively) renamed to "atx_heading",
# raising AttributeError on any --to html export. Found during Phase 2.
mistune==3.0.2
